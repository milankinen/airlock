#!/bin/bash
#MISE description="Build airlockd supervisor for Linux (musl)"
#MISE sources=["crates/airlockd/src/**/*.rs", "crates/airlockd/Cargo.toml", "crates/common/src/**/*.rs", "crates/common/schema/**/*.capnp", "crates/airlockd/builder.dockerfile"]
#MISE outputs=["target/vm/airlockd"]
set -euo pipefail

HOST_ARCH=$(uname -m)
case "$HOST_ARCH" in
  x86_64)  TARGET=x86_64-unknown-linux-musl ;;
  *)       TARGET=aarch64-unknown-linux-musl ;;
esac

mkdir -p target/vm

if [[ "${SUPERVISOR_BUILD_HOST:-}" == "true" ]]; then
  echo "Building airlockd (native, ${TARGET})..."
  cargo build --release -p airlock-supervisor --target "${TARGET}"
  cp "target/${TARGET}/release/airlockd" target/vm/airlockd
else
  echo "Preparing airlockd builder..."
  docker build --progress=quiet -t airlock-supervisor-builder -f crates/airlockd/builder.dockerfile crates/airlockd
  echo "Building airlockd (docker, ${TARGET})..."
  docker run --rm \
    -e "HOST_UID=$(id -u)" -e "HOST_GID=$(id -g)" \
    -v "$PWD:/workspace:ro" \
    -v "$PWD/target/vm:/out" \
    -v airlock-cargo-cache:/cargo-cache \
    -v airlock-target-cache:/target \
    airlock-supervisor-builder sh -c "
      export CARGO_HOME=/cargo-cache
      export RUSTUP_HOME=/cargo-cache
      cd /workspace
      rustup target add ${TARGET}
      cargo build --release -p airlock-supervisor --target ${TARGET} --target-dir /target
      cp /target/${TARGET}/release/airlockd /out/airlockd
      chown \${HOST_UID}:\${HOST_GID} /out/airlockd
    "
fi

echo "airlockd: target/vm/airlockd ($(du -h target/vm/airlockd | cut -f1))"
