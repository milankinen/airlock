<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>com.apple.security.virtualization</key>
    <true/>
    <key>com.apple.security.hypervisor</key>
    <true/>
    <!--
      LuaJIT (used by mlua for network middleware scripts) allocates
      executable pages for its traces. Under hardened runtime the
      kernel SIGKILLs the process on the first JIT'd dispatch unless
      this entitlement is present. Symptom: "Code Signature Invalid"
      / Instruction Abort / Permission fault a few minutes into a
      session with active HTTP middleware.
    -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
</dict>
</plist>
