presets = ["debian", "rust", "claude-code", "openai-codex", "copilot-cli"]

[vm]
image = "airlock:dev"

[network]
policy = "deny-by-default"

[network.ports.dev]
host = [40000, 18000]
guest = ["40080:8080"]

[daemons.tick]
command = ["sh", "-c", "while :; do echo tick; sleep 1; done"]
